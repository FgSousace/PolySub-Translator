## PolySub Translator 0.5.3

- naprawiono przygotowanie prywatnego środowiska AMD ROCm na Windows przez jawne
  instalowanie `setuptools` i `wheel` przed oficjalnym PyTorch ROCm 7.14;
- przerwana instalacja AMD jest automatycznie kontynuowana po ponownym uruchomieniu;
- RX 9070 XT jest izolowany od iGPU i oznaczany jako gotowy dopiero po teście obliczeń;
- wyłączono UPX, zastąpiono `get-pip.py` zweryfikowanym kołem pip i dodano skan
  Microsoft Defender przed publikacją;
- uporządkowano osobne informacje i nazwy plików każdego wydania.
